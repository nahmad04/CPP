#include <iostream>
using namespace std;

int main()
{
    int intMax = INT_MAX;
    cout << intMax << endl;
    cout << intMax + 1;
    //Goes from max value to min value just by adding one, works like a clock
    //Clock goes 10, 11, 12, the max value, and then goes to 1, the min value
    
    return 0;
}